/* Membership Page Specific Styles */
.membership-page-content {
    font-family: var(--body-font);
    color: var(--dark-text);
    background-color: var(--light);
}

/* Specific heading colors for different sections */
body.page-template-page-memberships .membership-hero h1,
body.page-template-page-memberships .membership-hero .subtitle {
    color: var(--white) !important; /* White for hero text */
}

body.page-template-page-memberships .section-title h2 {
    color: var(--primary) !important; /* Gold for section titles */
}

body.page-template-page-memberships .membership-card h3 {
    color: var(--primary) !important; /* Gold for card headings */
}

body.page-template-page-memberships .benefit-card h3 {
    color: var(--primary) !important; /* Gold for benefit headings */
}

body.page-template-page-memberships p {
    color: var(--gray); /* Standard gray for paragraphs */
}

/* Hero Section */
.membership-hero {
    min-height: 650px;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.membership-hero .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.4);
    z-index: 1;
}

.membership-hero .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: white;
}

.membership-hero h1 {
    color: white;
    margin-bottom: 20px;
}

.membership-hero .subtitle {
    color: rgba(255, 255, 255, 0.9);
    font-size: 1.3rem;
    margin-bottom: 30px;
}

/* Membership Grid */
.membership-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 30px;
    margin-top: 60px;
}

.membership-card {
    background: white;
    border-radius: 10px;
    padding: 40px 30px;
    box-shadow: 0 8px 40px rgba(0,0,0,0.07);
    transition: all 0.3s ease;
    text-align: center;
    position: relative;
    overflow: hidden;
    border: 1px solid var(--light-gray);
}

.membership-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 45px rgba(0,0,0,0.12);
}

.membership-card-header {
    margin-bottom: 30px;
}

.membership-card-header h3 {
    font-size: 1.8rem;
    margin-bottom: 10px;
    color: var(--primary) !important; /* Gold like home */
}

.membership-card-header .plan-type {
    color: var(--gray);
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.membership-card-price {
    margin-bottom: 30px;
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 5px;
}

.membership-card-price .currency {
    font-size: 1.5rem;
    color: var(--primary);
}

.membership-card-price .amount {
    font-size: 3rem;
    font-weight: 600;
    color: var(--primary);
}

.membership-card-price .period {
    color: var(--gray);
    font-size: 1rem;
}

.membership-card-features {
    margin-bottom: 30px;
    text-align: left;
}

.membership-card-features ul {
    list-style: none;
    padding: 0;
}

.membership-card-features li {
    padding: 10px 0;
    padding-left: 30px;
    position: relative;
    color: var(--dark-text);
}

.membership-card-features li:before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--primary);
    font-weight: bold;
}

/* Benefits Grid */
.benefits-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 40px;
    margin-top: 60px;
}

.benefit-card {
    text-align: center;
    padding: 30px;
    background-color: var(--white);
    border-radius: var(--border-radius-small);
    box-shadow: var(--box-shadow-light);
    transition: transform var(--transition-medium), box-shadow var(--transition-medium);
    border: 1px solid var(--light-gray);
}

.benefit-card:hover {
    transform: translateY(-6px);
    box-shadow: var(--box-shadow-medium);
}

.benefit-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--primary-ultralight);
    border-radius: 50%;
    font-size: 2.5rem;
    color: var(--primary);
}

.benefit-icon img {
    max-width: 50px;
    height: auto;
}

.benefit-card h3 {
    font-size: 1.4rem;
    margin-bottom: 15px;
    color: var(--primary) !important; /* Gold like home */
}

/* FAQ Accordion */
.faq-accordion {
    max-width: 800px;
    margin: 60px auto 0;
}

.faq-item {
    background: white;
    border-radius: 8px;
    margin-bottom: 15px;
    border: 1px solid var(--primary-ultralight);
    overflow: hidden;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

.faq-item.active {
    border-color: var(--primary); /* Gold active border */
    box-shadow: 0 5px 15px rgba(175, 143, 90, 0.1);
}

.faq-question {
    padding: 25px 30px;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background-color 0.3s ease;
}

.faq-question:hover {
    background-color: var(--primary-ultralight);
}

.faq-question h4 {
    margin: 0;
    font-size: 1.2rem;
    color: var(--dark-text);
}

.faq-toggle {
    font-size: 1.2rem;
    color: var(--primary);
    transition: transform 0.3s ease;
}

.faq-item.active .faq-toggle {
    transform: rotate(135deg); /* Match home page */
}

.faq-answer {
    padding: 0 30px;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, padding 0.3s ease;
}

.faq-item.active .faq-answer {
    padding: 0 30px 25px;
    max-height: 500px;
}

/* CTA Section */
.cta-section {
    background-color: var(--primary-ultralight);
    padding: 100px 0;
    text-align: center;
}

.cta-content h2 {
    margin-bottom: 20px;
    color: var(--primary); /* Gold heading */
}

.cta-buttons {
    margin-top: 30px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
}

/* Use consistent button styles from home page */
.cta-buttons .cta-button {
    background-color: var(--white);
    color: var(--dark-text);
    border-color: var(--primary);
    box-shadow: 0 4px 15px rgba(175, 143, 90, 0.15);
}

.cta-buttons .cta-button:hover {
    background-color: var(--primary-dark);
    border-color: var(--primary-dark);
    color: var(--white);
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 8px 20px rgba(175, 143, 90, 0.25);
}

.cta-buttons .cta-button-secondary {
    background-color: transparent;
    color: var(--primary);
    border-color: var(--primary);
}

.cta-buttons .cta-button-secondary:hover {
    background-color: var(--primary);
    color: var(--white);
    border-color: var(--primary);
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 8px 20px rgba(175, 143, 90, 0.2);
}

/* Responsive */
@media (max-width: 768px) {
    .membership-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .membership-card {
        padding: 30px 20px;
    }
    
    .membership-card-price .amount {
        font-size: 2.5rem;
    }
    
    .benefits-grid {
        grid-template-columns: 1fr;
        gap: 30px;
    }
    
    .faq-question {
        padding: 20px;
    }
    
    .faq-question h4 {
        font-size: 1.1rem;
    }
}